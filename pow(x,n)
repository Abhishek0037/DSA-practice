class Solution(object):
    def myPow(self, x, n):
        result=1
        nc=abs(n)
        while nc>0:
            if nc%2 == 1:
                result *= x
                nc=nc-1
            else:
                nc=nc//2
                x=x*x
        return result if n >= 0 else 1 / result
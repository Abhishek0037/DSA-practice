class Solution(object):
    def maxSubArray(self, nums):
        curr = 0
        maxi= float('-inf')
        for num in nums:
            curr+= num
            maxi=max(maxi,curr)
            if curr<0:
                curr=0
        return maxi
      
